/**
 * This file is deprecated.
 * We are now using an embedded SQLite database (`prisma/dev.db`) for local testing 
 * without network restrictions.
 * All database queries are handled through the Prisma Client instance.
 */
module.exports = {};
